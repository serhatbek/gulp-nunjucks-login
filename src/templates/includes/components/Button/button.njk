{% macro item(text, type, class, href, disabled, buttonv2) %}
  {% if href %}
    <a href="{{ href }}" class="button
      {%- if class %} {{ class }} {%- endif -%}
      {%- if buttonv2 %} button--v2{%- endif -%}
    ">{{ text }}</a>
  {% else %}
    <button class="button
      {%- if class %} {{ class }} {%- endif -%}
      {%- if buttonv2 %} button--v2{%- endif -%}
    "
    type="{%- if type -%} {{type}}{% else %}button{%- endif -%}"
    {%- if disabled -%} disabled {%- endif -%}
    >{{ text }}</button>
  {% endif %}
{% endmacro %}